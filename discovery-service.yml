server:
  port: 8761

spring:
  application:
    name: discovery-service

  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        register: true
        health-check-path: /actuator/health
        health-check-interval: 10s
